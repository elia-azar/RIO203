Finish the implementation of centrale
Create a Docker container that will host a DB
Create a Service to access the container and store data in the DB
Check if multi-threading creates a deadlock or generates errors if too many calls to the same DB connector
implement gethome, gethome_from_db